print("Welcome to tip calculator!")
bill = float(input("What was the total bill? $"))
tip = int(input("How much tip would you like to give today? 10, 12, or 15? "))
people = int(input("How many people to split the bill with?"))
bill_with_tip = tip/100 * bill + bill
split = float(bill_with_tip/people)
final_amount = round(split, 2)
print(f"Each person will pay: ${final_amount}" )
